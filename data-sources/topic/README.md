# Q Topic

Creates a QuickSight Q Topic linked to a dataset, enabling natural language querying.

Uses the `AWS::QuickSight::Topic` CloudFormation resource via Terraform CFN bridge since there's no native Terraform resource for topics.
